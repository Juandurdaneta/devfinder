import React from "react";
import { Wrapper, Container } from "./Header.styles";

const Header = () => (
    <Wrapper>
        <Container>
            <h2>devfinder</h2>
        </Container>
    </Wrapper>
);

export default Header;